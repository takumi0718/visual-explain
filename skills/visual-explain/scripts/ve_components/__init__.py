"""visual-explain semantic component foundation (generation-time only).

This package is dark generation-time code: importing it changes no legacy
output. It owns the authoring contracts, deterministic component selection,
mixed-document assembly, static renderers, and the four-layer checker used to
build accessible, self-contained explainer documents.
"""
