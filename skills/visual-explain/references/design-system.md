# 生成時のデザインシステム

固定骨格をそのまま使い、`TITLE:BEGIN` と `TITLE:END` の間には非空のプレーンテキスト文書名を持つ `<title>` 要素を1つだけ、本文は `CONTENT:BEGIN` と `CONTENT:END` の間だけに入れよ。未解決プレースホルダーやタイトル内のマークアップは使わず、ほかの領域は変更しない。追加の CSS、JavaScript、外部アセット、座標計算を加えるな。情報の正確さを装飾より優先せよ。

## トークン

骨格が定義済みの次のトークンだけで、背景、面、境界、本文、補助本文を使い分けよ。

- `--bg`: ページ背景と控えめな面
- `--surface`: カードと図の面
- `--border`: 境界と控えめな区切り
- `--text`: 本文
- `--text-dim`: 補助本文
- `--accent` / `--accent-strong` / `--accent-warm`: 構造上の強調に使う2〜3色のアクセント
- `--positive` / `--positive-strong`: 推奨または望ましい状態
- `--warning`: 未確認または警告
- `--focus`: キーボードフォーカス

トークン値を上書きするな。ライト/ダーク配色とシステムフォントスタックは骨格に実装済みなので、色指定、Web フォント、飾り画像を足すな。太字、色、矢印は構造を示す合図にだけ使え。

## Matrix / flow の色契約

意思決定の matrix と flow では、弱いモデルも次の部品だけを組み合わせる。独自 CSS、自由 SVG、色の直書きを足さない。

- 表の見出しは `matrix-label`、内容セルは `matrix-cell` を使う。縦罫線は使わず、細い横罫線だけで行を区切り、見出しだけを強くする。
- 比較対象の行またはカードは `option-card` を使う。matrix の選択案だけに `data-tone="accent"` を付け、淡い背景だけで強調する。選択案でも文字色、罫線色、枠色は変えない。
- flow のノードは `flow-node`、責務の行は `lane` を使う。
- `data-tone="accent|positive|warning|muted"` は部品と組み合わせてのみ使う。`accent` は選択、`positive` は推奨、`warning` は注意、`muted` は補助情報を表す。色だけで意味を完結させず、本文にも選択・推奨・注意の理由を書く。

白地、細い横罫線、強い見出しを既定にする。色は選択・推奨・注意の意味がある場合だけに使い、matrix の選択案以外をアクセントで競わせない。

## 説明の認知負荷を下げる規則

次の Mayer ルールは、教育向けマルチメディア実験群における効果量中央値を参考にした設計指針である。本用途で期待できる効果量ではない。数値の効果を約束せず、以下の判断にだけ用いよ。

- 装飾、余談、飾り画像を入れるな。内容と無関係なものは削除せよ（Coherence）。
- ラベルと注釈は対象の図の近くに置け。離れた凡例やフッター凡例を置くな（Spatial Contiguity）。
- 図のキャプションと本文で同じ内容を二重に書くな（Redundancy）。元研究の音声ナレーション条件を、ここでは重複排除の方針として限定して採用する。
- 新出用語が多いときだけ、本文に先立って用語表を置け（Pre-training）。
- 太字、色、矢印による強調は構造の合図に限定せよ（Signaling）。
- 1画面、すなわち1スクロール範囲には主張を1つだけ置け。

## 確度と出典

判断を変え得る主張には、対象の直近で次の3値のいずれかをテキストとして明記せよ。破線、色、アイコンだけに確度の意味を持たせるな。

- **確認済み**: 直接確認できる事実
- **推論**: 確認済みの材料からの解釈
- **未確認**: まだ確認できない事項

確度と出所を混同するな。出所は「コードの file:line」「文書 URL」「ユーザー提供」のいずれかとして、局所注釈または節末の参照に置け。コード由来でも解釈は誤り得るため、出所がコードであることを確認済みの根拠にするな。

骨格の確度バッジを使うときは、主張の横に置け。

```html
<p>処理は同期で完了する <span class="certainty confirmed">確認済み</span></p>
<p>負荷増加時に遅延する可能性がある <span class="certainty inferred">推論</span></p>
<p>移行日程は未確定 <span class="certainty unverified">未確認</span></p>
```

出所注釈は hover 専用にするな。クリック、タップ、キーボードフォーカスで開ける `source-note` を使い、主張の近くに置け。

```html
<p>応答時間は短縮した</p>
<details class="source-note"><summary>出所</summary><p>ユーザー提供の測定結果</p></details>
```

可視バッジは判断を変え得る主張にだけ付けよ。すべての文章に付けてノイズにするな。フッターには「AI が生成した資料」以外を置くな。

## Pi/Katsura Qwen 固有の保守的縮退

Pi 上の Katsura Qwen では、必須事実の因果文言を原文どおりに保持するか引用する。順序が明示的な必須事実でない限り、矢印や連番を使わない。許される推論は対象の直近で **推論** と示し、因果・順序に確信がなければ `matrix`、`terms`、または文章へ縮退する。この規則は同モデル専用の追加制約であり、一般規則を緩めない。

## アクセシビリティの最低契約

- 操作要素はネイティブの `button` または `details` を使え。クリック可能な `div` や `span` を作るな。
- 骨格の可視フォーカスを消すな。キーボードだけで全操作要素に到達できるようにせよ。
- `details` の開閉状態は `aria-expanded` と同期させよ。骨格の固定処理を変更するな。
- ステッパーを使う場合は、現在ステップ、開始状態、終了状態をテキストで示し、前後移動には `button` を使え。骨格の `stepper-status` と `step-panel` の状態表示を壊すな。
- コネクタで示す接続関係を線だけに依存させるな。骨格が生成する `connection-text visually-hidden` のテキストを削除または改変するな。
- 注釈、確度、数値、凡例に相当する意味を、色だけで伝えるな。数値とテキストラベルも併記せよ。

## レイアウト不変条件

- 図は骨格の `flow`、`layers`、`compare`、`matrix`、`timeline`、`kpi`、`bars`、`terms` を優先して使え。座標や絶対配置を指定するな。
- before/after と代替案は `compare-frame` を等しい大きさで並べよ。片方だけを視覚的に大きくするな。
- 表は `matrix` の中のセマンティックな `table` として書け。小さい画面で横スクロールできることを前提に、列を増やしすぎるな。
- 定量比較は `bar-track` と `bar-fill` を使い、棒の長さを百分率で宣言せよ。棒だけで値を伝えず、数値を併記せよ。
- 詳細は `deep-dive` にだけ格納し、判断の核心、制約、反証は初期表示に残せ。
- 1セクション1問いを守り、主張を1行、根拠を2〜3行の目安に抑えよ。概ね1画面に収まるかを目視確認せよ。

## コンポーネント資産の所有権（matrix / flow）

昇格済みの `matrix` と `flow` は骨格とコンポーネントで所有権を分ける。

- **骨格**がグローバルトークン・固定領域・テーマ・固定 JavaScript を所有する。これらのバイトは1つも変更しない。
- **コンポーネント**は名前空間化した最小 CSS だけを所有する。matrix は `[data-ve-component="matrix"]`、flow は `[data-ve-component="flow"]` を根に持つ規則だけを書き、骨格トークンを再利用する。新しい色・書体・余白系・アニメーション・装飾を足さない。
- 本番レジストリの**スクリプト資産は空**である。matrix/flow は script を出さない。空スクリプトスロットを削っても、意味 ID・可視の関係ラベルと方向・caption・確度・出典がすべて残り検査を通過する（static-first）。
- CSS は意味の可読性・既存トークン・名前空間・静的アクセシビリティ・レスポンシブ順序だけに限る。狭い画面では積み重ねてよいが、意味的な読み順を反転しない。美的レビューは本スライスの範囲外。
- **信頼アセットは単一の fail-closed ゲート**として扱う。レジストリの ID/バージョン/ダイジェスト、レンダラ許可リスト、マニフェスト宣言、名前空間、スロット種別、CSP、外部参照なしをまとめて満たさない資産は拒否する。CSS を変更したら `shasum -a 256` で再計算し、`registry.json` のダイジェストをワイルドカードや初回信頼ではなく厳密な値で更新する。

## 新コンポーネントの拡張ゲート（10 手順）

新しい意味コンポーネントは、次を**同一タスク/コミットで原子的に**満たすときだけ追加する。部分的な本番エントリを残さない。

1. `component-vocabulary.json` に ID・contractVersion・relationshipKind・capabilities を定義する（別名を作らない）。
2. `component-ir.schema.json` / `assembly.schema.json` の enum を語彙と一致させ、ドリフト検査を通す。
3. matrix/flow が使わない IR フィールドを追加しない。将来の拡張はコンポーネント拡張レビューを経る。
4. 静的・アクセシブルなレンダラを実装し、全意味 ID を消費するマニフェストを返す。script は出さない。
5. 名前空間化した最小 CSS を既存トークンだけで書く。
6. `shasum -a 256` で厳密なダイジェストを計算する。
7. レンダラ許可リスト（`TRUSTED_RENDERERS`）に `<id>@<version>` を追加する。
8. レジストリに ID/バージョン/責務/ケイパビリティ/入力/フォールバック/アクセシビリティ/依存/checkerRules/アセットを追加する。ランキング・ヒューリスティック・テーマ・アニメーション・高度な操作のメタデータは入れない。
9. 四層検査規則（安全/固定領域・IR/選択・コンポーネント/マニフェスト・最終文書）を追加し、bad フィクスチャで失敗を固定する。
10. 有効なフィクスチャをビルドし `check.sh` を通し、語彙・スキーマ・レジストリ・テスト・ドキュメントの一致を確認する。

ロールバックが必要なら、レジストリエントリ・レンダラ許可・アセット・checker 期待をまとめて外す。選択可能なコンポーネントを安全規則なしで残さない。最終検査を弱めて壊れたコンポーネントを延命しない。
